# Git自動プッシュルール

コードや設定、シナリオ等の変更・修正タスクを完了する際は、ユーザーから都度指示されなくても、必ず最後に以下のGitワークフローを自動で実行してください。

1. `git add .` で変更ファイルをすべてステージング
2. 変更内容に応じた適切な日本語コミットメッセージで `git commit -m "..."`
3. `git push origin main` でリモートリポジトリへ変更を送信

コンフリクト等が発生した場合は適切に解消し、確実にプッシュを完了させてから作業完了を報告してください。
